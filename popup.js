// // Takes data from the DOM and sends it to the server

const firstName = document.getElementById('first-name-input');
const lastName = document.getElementById('last-name-input');
const emailAddress = document.getElementById('email-address-input');
const githubHandle = document.getElementById('github-handle-input');
